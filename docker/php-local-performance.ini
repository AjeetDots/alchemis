; Local Docker: fewer filesystem stats per request (bind mounts on Windows/macOS are slow).
; revalidate_freq=0 in the image means OPcache checks every script on every request.
opcache.revalidate_freq=2
opcache.validate_timestamps=1

realpath_cache_size=4096K
realpath_cache_ttl=600
